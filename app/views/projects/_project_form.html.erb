<%= form_for(current_user.projects.build, remote: true) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <%= button_tag(type: "submit", class: "btn btn-large btn-primary") do %>
    <%= fa_icon "plus" %> Add TODO List
  <% end %>
<% end %>