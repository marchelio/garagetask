<% if @project.save %>
  $(".projects").append("<%= escape_javascript(render(@project)) %>");
<% else %>
  <% form_for(@project) do |f| %>
    $(".projectform").append("<%= escape_javascript(render 'shared/error_messages', object: f.object) %>");
  <% end %> 
<% end %>